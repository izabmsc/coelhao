# Aprendendo GIT/GITHUB
Esse é meu primeiro projeto em git, com ele  vou subir todos os outros projetos que eu tenho na minha máquina e deixar como portifólio

foram utilizados os comandos:
git init
git add 
git status
git commit -m "mensagem do commit"
git push
git branch
git checkout nomedabranch
bit merge nomedabranch
git pull

OBS: utilizado também o comando 
touch .gitignore - para ignorar arquivos/pastas que não irão ser compartilhadas na nuvem

E é isso, meu primeiro projeto, logo estarei subindo outros :)
